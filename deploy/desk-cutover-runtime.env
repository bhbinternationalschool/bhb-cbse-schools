# Runtime flags applied to the Cloud Run service after deploy.
# Committed source of truth for the trigger (no .env.local in the cloud).
# The desk-cutover flags are all "true" — cutover complete, verified against
# the live revision 2026-08-23. ERP_WA_COMMANDS is not one of them; see below.
ACCOUNTS_DUAL_WRITE_DB=true
ACCOUNTS_READ_FROM_DB=true
ADMISSIONS_DUAL_WRITE_DB=true
ADMISSIONS_READ_FROM_DB=true
ATTENDANCE_DUAL_WRITE_DB=true
ATTENDANCE_READ_FROM_DB=true
AUTOMATION_DUAL_WRITE_DB=true
AUTOMATION_READ_FROM_DB=true
CERTIFICATES_DUAL_WRITE_DB=true
CERTIFICATES_READ_FROM_DB=true
# ERP_WA_COMMANDS is deliberately ABSENT. The variable is a kill switch, not
# an on switch — unset means the ERP command desk is ON — so the
# `ERP_WA_COMMANDS=off` line that used to sit here has been removed rather
# than commented out. WHO it is on for is decided by the allowlist below,
# which is absent as of 2026-09-08 — school-wide — not by this variable.
#
# It has to be removed HERE, not just on the service. cloudbuild.yaml's
# --set-env-vars replaces the whole environment and this file is re-applied
# additively on every build, so a `gcloud run services update` that switched
# it on by hand would be undone by the next deploy. That is exactly what kept
# the desk off after the 2026-09-07 deploy while everyone assumed it was on.
#
# ERP_WA_COMMANDS_ALLOW is deliberately ABSENT — the desk is SCHOOL-WIDE,
# decided 2026-09-08. An absent or empty allowlist means every staff member
# whose mobile is on an active staff record, which at BHB is 28 people.
#
# The pilot line that used to sit here has been removed rather than
# commented out, for the same reason ERP_WA_COMMANDS was: this file is
# re-applied additively on every build, so a commented line is not a
# smaller version of the setting — it is the setting gone. Removing it
# says so plainly.
#
# To narrow it again, put the line back with the mobiles that may use it:
#   ERP_WA_COMMANDS_ALLOW=<comma-separated mobiles>
# Note the failure direction: a typo that empties the variable opens the
# desk to everyone rather than closing it to nobody. That is the safer
# failure now that school-wide IS the intent, and it was the dangerous one
# while a pilot was. See docs/ERP_COMMANDS_ROLLOUT.md §4.
ERP_CHAT_DUAL_WRITE_DB=true
ERP_CHAT_READ_FROM_DB=true
EXAMS_DUAL_WRITE_DB=true
EXAMS_READ_FROM_DB=true
EXAM_PAPERS_DUAL_WRITE_DB=true
EXAM_PAPERS_READ_FROM_DB=true
FEES_DUAL_WRITE_DB=true
FEES_READ_FROM_DB=true
FEE_RECOVERY_TASKS_DUAL_WRITE_DB=true
FEE_RECOVERY_TASKS_READ_FROM_DB=true
GALLERY_DUAL_WRITE_DB=true
GALLERY_READ_FROM_DB=true
HOMEWORK_DUAL_WRITE_DB=true
HOMEWORK_READ_FROM_DB=true
LIBRARY_DUAL_WRITE_DB=true
LIBRARY_READ_FROM_DB=true
MASTERS_DUAL_WRITE_DB=true
MASTERS_READ_FROM_DB=true
MODULE_REGISTRY_DUAL_WRITE_DB=true
MODULE_REGISTRY_READ_FROM_DB=true
NEWS_DUAL_WRITE_DB=true
NEWS_READ_FROM_DB=true
NEXT_PUBLIC_ACCOUNTS_READ_FROM_DB=true
NEXT_PUBLIC_ADMISSIONS_READ_FROM_DB=true
NEXT_PUBLIC_ATTENDANCE_READ_FROM_DB=true
NEXT_PUBLIC_AUTOMATION_READ_FROM_DB=true
NEXT_PUBLIC_CERTIFICATES_READ_FROM_DB=true
NEXT_PUBLIC_ERP_CHAT_READ_FROM_DB=true
NEXT_PUBLIC_EXAMS_READ_FROM_DB=true
NEXT_PUBLIC_EXAM_PAPERS_READ_FROM_DB=true
NEXT_PUBLIC_FEES_READ_FROM_DB=true
NEXT_PUBLIC_FEE_RECOVERY_TASKS_READ_FROM_DB=true
NEXT_PUBLIC_GALLERY_READ_FROM_DB=true
NEXT_PUBLIC_HOMEWORK_READ_FROM_DB=true
NEXT_PUBLIC_LIBRARY_READ_FROM_DB=true
NEXT_PUBLIC_MASTERS_READ_FROM_DB=true
NEXT_PUBLIC_MODULE_REGISTRY_READ_FROM_DB=true
NEXT_PUBLIC_NEWS_READ_FROM_DB=true
NEXT_PUBLIC_NOTIFICATIONS_READ_FROM_DB=true
NEXT_PUBLIC_PAYMENTS_READ_FROM_DB=true
NEXT_PUBLIC_PAYROLL_READ_FROM_DB=true
NEXT_PUBLIC_PTM_READ_FROM_DB=true
NEXT_PUBLIC_PURCHASE_READ_FROM_DB=true
NEXT_PUBLIC_RBAC_READ_FROM_DB=true
NEXT_PUBLIC_RTE_READ_FROM_DB=true
NEXT_PUBLIC_SCHOOL_COMMS_READ_FROM_DB=true
NEXT_PUBLIC_SIS_READ_FROM_DB=true
NEXT_PUBLIC_STAFF_ADVANCES_READ_FROM_DB=true
NEXT_PUBLIC_STAFF_ATTENDANCE_READ_FROM_DB=true
NEXT_PUBLIC_STAFF_CHAT_READ_FROM_DB=true
NEXT_PUBLIC_STAFF_HR_READ_FROM_DB=true
NEXT_PUBLIC_STAFF_READ_FROM_DB=true
NEXT_PUBLIC_STORE_READ_FROM_DB=true
NEXT_PUBLIC_STUDENT_LEAVE_READ_FROM_DB=true
NEXT_PUBLIC_TIMETABLE_READ_FROM_DB=true
NEXT_PUBLIC_TRANSPORT_READ_FROM_DB=true
NEXT_PUBLIC_TRUST_READ_FROM_DB=true
NEXT_PUBLIC_VAULT_READ_FROM_DB=true
NEXT_PUBLIC_WA_TEMPLATES_READ_FROM_DB=true
NEXT_PUBLIC_WA_THREADS_READ_FROM_DB=true
NOTIFICATIONS_DUAL_WRITE_DB=true
NOTIFICATIONS_READ_FROM_DB=true
PAYMENTS_DUAL_WRITE_DB=true
PAYMENTS_READ_FROM_DB=true
PAYROLL_DUAL_WRITE_DB=true
PAYROLL_READ_FROM_DB=true
PTM_DUAL_WRITE_DB=true
PTM_READ_FROM_DB=true
PURCHASE_DUAL_WRITE_DB=true
PURCHASE_READ_FROM_DB=true
RBAC_DUAL_WRITE_DB=true
RBAC_READ_FROM_DB=true
RTE_DUAL_WRITE_DB=true
RTE_READ_FROM_DB=true
SCHOOL_COMMS_DUAL_WRITE_DB=true
SCHOOL_COMMS_READ_FROM_DB=true
SIS_DUAL_WRITE_DB=true
SIS_READ_FROM_DB=true
STAFF_ADVANCES_DUAL_WRITE_DB=true
STAFF_ADVANCES_READ_FROM_DB=true
STAFF_ATTENDANCE_DUAL_WRITE_DB=true
STAFF_ATTENDANCE_READ_FROM_DB=true
STAFF_CHAT_DUAL_WRITE_DB=true
STAFF_CHAT_READ_FROM_DB=true
STAFF_DUAL_WRITE_DB=true
STAFF_HR_DUAL_WRITE_DB=true
STAFF_HR_READ_FROM_DB=true
STAFF_READ_FROM_DB=true
STORE_DUAL_WRITE_DB=true
STORE_READ_FROM_DB=true
STUDENT_LEAVE_DUAL_WRITE_DB=true
STUDENT_LEAVE_READ_FROM_DB=true
TIMETABLE_DUAL_WRITE_DB=true
TIMETABLE_READ_FROM_DB=true
TRANSPORT_DUAL_WRITE_DB=true
TRANSPORT_READ_FROM_DB=true
TRUST_DUAL_WRITE_DB=true
TRUST_READ_FROM_DB=true
VAULT_DUAL_WRITE_DB=true
VAULT_READ_FROM_DB=true
WA_TEMPLATES_DUAL_WRITE_DB=true
WA_TEMPLATES_READ_FROM_DB=true
WA_THREADS_DUAL_WRITE_DB=true
WA_THREADS_READ_FROM_DB=true
